export const enum BlockType {
  DISPLAY = "DISPLAY",
  OPERATORS = "OPERATORS",
  NUMPAD = "NUMPAD",
  EQUALS = "EQUALS",
}

export const enum CanvasStateType {
  RUNTIME = "RUNTIME",
  CONSTRUCTOR = "CONSTRUCTOR",
}
